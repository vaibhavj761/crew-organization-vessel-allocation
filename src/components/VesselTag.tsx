import type{Vessel}from'../types'
const label=(s:string)=>s.replaceAll('_',' ').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase())
export function VesselTag({vessel,detailed=false}:{vessel:Vessel;detailed?:boolean}){return <div className="vessel-tag"><span className="vessel-marker"/><span className="vessel-main"><strong>{vessel.name||'Unnamed vessel'}</strong><small>{detailed?[vessel.vesselType,vessel.vesselDoc,vessel.ownerPool].filter(Boolean).join(' • '):vessel.vesselType||'Vessel'}</small></span><span className="vessel-badges"><i>{label(vessel.vesselStatus)}</i>{detailed&&<i>{label(vessel.managementType)}</i>}</span></div>}
